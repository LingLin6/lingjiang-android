# 实时听觉分析助手完整方案

## 一句话目标

做一个运行在手机上的实时听觉分析助手。用户点击开始后，手机像接通电话一样持续监听周边声音，实时完成语音识别、上下文理解、分析建议、可选语音提醒；点击挂断后，自动生成完整会话记录、摘要、统计和可回看的分析文件。

未来可以把手机显示替换成 AR 眼镜信息浮窗，并进一步加入视觉输入。

## 核心判断

这个思路可行，而且路线比较清晰。它的本质不是普通录音、实时字幕或会议纪要，而是一个带上下文记忆的实时对话分析系统。

关键能力有四个：

- 实时听见：手机持续采集音频并转成文字
- 实时理解：大模型持续结合上下文分析当前对话
- 实时提醒：用文字或语音给出建议、风险和追问点
- 事后沉淀：挂断后生成可检索、可导出的完整记录

## 对豆包手机端电话形态的借鉴

可以借鉴的是“通话外壳”，不是“对话内核”。

值得参考：

- 一键接通和挂断
- 通话中计时、波形、状态提示
- 耳机、免提、静音、暂停等控制
- 后台持续运行和锁屏状态提醒
- 像通话记录一样管理历史会话
- 挂断后进入会话总结和记录页

不能照搬：

- 不做成用户和 AI 轮流说话的聊天电话
- 不让 AI 默认参与真实对话或抢话
- 不把每一句话都当成给 AI 的指令
- 不追求陪聊，而追求旁路理解、分析和提醒

更准确的定位是：AI 在旁边持续听、持续理解，在必要时给出少量高价值提示。

## 核心链路

```text
点击开始
  ↓
手机麦克风持续采集周边声音
  ↓
音频分片与降噪处理
  ↓
实时语音识别
  ↓
更新字幕、短期上下文、长期摘要
  ↓
大模型结合上下文分析
  ↓
手机显示实时结果
  ↓
可选语音播报关键建议
  ↓
点击挂断
  ↓
生成完整会话报告与统计
```

## 产品形态

手机端提供一个类似通话的界面：

- 开始：进入持续监听和实时分析
- 挂断：结束监听并生成报告
- 暂停：临时停止采集，但保留当前会话
- 静音分析：只显示文字，不语音播报
- 语音提醒：通过耳机播报关键建议
- 历史记录：查看过去每次会话的报告和转写

实时页面建议包含：

- 实时字幕：当前正在听到的内容
- 当前摘要：最近一段对话的核心意思
- 分析建议：风险、追问、行动建议
- 会话状态：已进行时长、识别状态、模型状态
- 播报开关：是否用语音提示建议

界面上不应以聊天气泡为主，而应以“当前状态 + 关键提示卡片 + 字幕记录”为主。这样能避免产品被误解成对话式 AI。

## 工作模式

建议把系统设计成几个清晰状态：

```text
待机
  等待用户点击开始。

监听中
  持续采音、识别、生成字幕。

分析中
  结合上下文生成摘要、建议、风险和追问点。

提醒中
  只在高价值事件出现时显示或播报建议。

暂停中
  暂停采集，保留当前会话上下文。

复盘中
  挂断后生成完整报告、统计和可回看记录。
```

这个状态设计比“始终和 AI 对话”更适合你的目标。

## 实时分析方式

大模型不能只分析最新一句话，而要结合完整会话上下文。

推荐维护三层上下文：

```text
实时字幕层
  保存完整转写文本，按时间切片。

短期窗口层
  保留最近 30-180 秒原文，用于理解当前语境。

长期摘要层
  持续压缩更早内容，保留背景、结论、分歧、风险、待办。
```

每轮分析输入：

```text
当前新内容
+ 最近短期窗口
+ 长期滚动摘要
+ 结构化关键信息
+ 当前场景模式
```

每轮分析输出：

```text
实时建议
+ 更新后的滚动摘要
+ 新增关键事实
+ 新增待办
+ 新增风险
+ 需要追问的问题
```

这样第五分钟的建议会基于前五分钟的对话脉络，而不是孤立分析第五分钟听到的一句话。

## 实时触发策略

不建议每识别一句话就完整调用一次大模型。这样会慢、贵、吵，也容易生成低价值提示。

更好的方式是“事件触发 + 定时压缩 + 优先级过滤”：

- 实时字幕：持续更新
- 短摘要：每 15-30 秒更新一次
- 滚动摘要：每 1-3 分钟压缩一次
- 实时建议：只在关键事件出现时生成
- 最终报告：挂断后集中生成

关键事件包括：

- 出现明确承诺、时间、价格、责任人
- 出现风险、分歧、拒绝、模糊说法
- 对方提出问题或要求你做决定
- 话题发生明显切换
- 长时间讨论后出现阶段性结论
- 你主动点击“分析当前局势”

每条建议都应有优先级：

```text
低：只记录，不打扰
中：显示在屏幕上
高：显示并可语音播报
```

这能让助手更像“关键时刻提醒”，而不是一直插话。

## 上下文压缩

长时间会话不能把所有原文都塞给模型，需要边听边压缩。

压缩策略：

- 每隔一段时间生成滚动摘要
- 重要原文保留，不重要内容压缩
- 待办、结论、风险单独结构化保存
- 发生口径变化时更新摘要，不简单追加
- 每条重要结论尽量保留对应时间戳和原文证据
- 最终报告基于完整转写、滚动摘要和结构化信息共同生成

建议保存的结构化状态：

```text
会话背景
主要人物
关键事实
已达成结论
未解决问题
待办事项
风险与分歧
重要时间点
模型实时建议
```

压缩后的摘要不能覆盖原始转写。完整转写应作为底账保存，摘要只是为了实时分析和最终报告提效。

## 语音建议

实时建议支持两种输出方式：

- 文字显示：默认开启
- 语音播报：默认关闭，用户主动开启

语音播报适合戴耳机时使用，像一个旁边的助手轻声提醒。

播报模式：

- 静默：只显示文字
- 关键提醒：只播报风险、待办、重要追问
- 全量建议：播报所有实时建议

必要控制：

- 音量调节
- 播报频率限制
- 一键暂停播报
- 只在耳机连接时启用
- 不打断当前录音和识别
- 避免反复播报同一类建议
- 避免把自己播报的语音再次识别成环境声音

建议默认只播报高优先级提醒，普通建议只显示文字。

## 存储与报告

每次开始监听时创建一条会话记录，挂断后结束并归档。

建议保存：

- 原始转写文本
- 分段时间戳
- 滚动摘要
- 结构化关键信息
- 实时建议记录
- 语音播报记录
- 最终报告
- 基础统计

基础数据结构：

```text
Session
  ├─ id
  ├─ title
  ├─ started_at
  ├─ ended_at
  ├─ duration
  ├─ scenario
  ├─ transcript_segments
  ├─ rolling_summary
  ├─ key_facts
  ├─ decisions
  ├─ open_questions
  ├─ action_items
  ├─ risks
  ├─ realtime_insights
  ├─ spoken_insights
  └─ final_report
```

挂断后生成的报告建议包含：

- 会话标题
- 时间、地点、时长
- 一句话总览
- 主要讨论内容
- 关键结论
- 待办事项
- 风险与分歧
- 重要时间线
- 实时建议回顾
- 完整转写记录
- 基础统计
- 关键结论对应的时间戳和原文依据

报告里要区分“确定事实”和“模型推断”。这会直接影响可信度。

## App 内查看

App 需要提供历史会话库：

- 按时间查看会话
- 搜索转写、摘要、待办和风险
- 查看完整报告
- 查看完整转写
- 给会话改标题、加标签、收藏
- 导出 Markdown、PDF 或纯文本

自用阶段建议优先本地存储，后续再考虑云端同步。

## 技术架构

```text
手机端
  ├─ 音频采集
  ├─ 降噪、回声抑制、语音活动检测
  ├─ 音频分片
  ├─ 实时语音识别接口
  ├─ 上下文管理器
  ├─ 事件检测器
  ├─ 大模型分析接口
  ├─ 提醒路由器
  ├─ 本地数据库
  ├─ 报告生成器
  ├─ 实时界面
  └─ 可选 TTS 播报
```

推荐方式：

- 手机端优先做 App，网页对长时间采集和后台权限限制更多
- 语音识别优先使用实时语音识别接口
- 大模型使用云端 API，手机负责调度和展示
- 自用情况下，密钥可放手机安全存储，不硬编码进源码
- 语音播报可先用系统 TTS，效果不够再换云端 TTS

优化后的核心流水线：

```text
音频
  ↓
降噪 / 语音活动检测 / 回声抑制
  ↓
实时语音识别
  ↓
转写分段
  ↓
事件检测
  ↓
上下文管理
  ↓
大模型分析
  ↓
提醒路由
  ├─ 屏幕显示
  ├─ 语音播报
  └─ 存入会话记录
```

## 主要难点

1. 实时性
   语音识别、模型分析、语音播报都需要控制延迟。建议目标是字幕 1-3 秒内更新，分析建议 3-10 秒内更新。

2. 上下文质量
   压缩不能丢掉关键事实，否则后续分析会跑偏。需要把结论、风险、待办、人物意图单独保存。

3. 信息打扰
   实时建议必须短而准。语音播报尤其要克制，否则会干扰真实对话。

4. 手机权限
   采集周边麦克风声音可行，但直接读取普通系统电话的对方声音会受到 iOS 和 Android 限制。

5. 成本与耗电
   长时间实时识别和调用模型会产生 API 成本，也会增加手机耗电和发热。

6. 隐私与合规
   涉及他人对话时应明确告知。自用也应避免采集敏感场景。

## 当前方案的缺陷与优化

1. 容易变成“AI 电话聊天”
   缺陷是交互如果做成聊天气泡，用户会下意识和 AI 对话。优化方向是使用通话式外壳，但主体界面放字幕、摘要、提示卡片和会话状态。

2. 实时建议可能太吵
   缺陷是模型每听到一句都分析会造成干扰。优化方向是事件触发、优先级过滤、频率限制，默认只显示文字，高优先级才播报。

3. 长上下文可能压缩失真
   缺陷是滚动摘要可能丢掉关键事实。优化方向是完整转写做底账，重要结论绑定时间戳和原文证据，结构化保存待办、风险、分歧和结论。

4. 语音识别错误会传染给分析
   缺陷是模型可能基于错误字幕继续推理。优化方向是保存识别置信度，对低置信内容标记“不确定”，报告中避免把不确定内容写成事实。

5. 手机麦克风采集范围有限
   缺陷是远距离、嘈杂环境、多人同时说话会影响效果。优化方向是支持耳机麦克风、领夹麦克风或外接麦克风，并加入降噪和说话人区分。

6. 语音播报可能形成回声污染
   缺陷是手机播报的建议可能被麦克风重新采集。优化方向是耳机优先、回声抑制、播报时短暂降低采集权重，并记录播报时间避免误识别。

7. API 成本和电量消耗会偏高
   缺陷是长时间实时识别和分析会持续耗费。优化方向是字幕持续、分析分级，普通阶段用轻量判断，关键事件再调用更强模型。

8. 真实系统电话音频不可控
   缺陷是 iOS 和 Android 通常不允许第三方 App 稳定读取普通电话对方声音。优化方向是明确只采集麦克风环境声，或做自己的实时语音会话。

9. 报告可信度可能不够
   缺陷是最终纪要如果只给结论，用户很难判断来源。优化方向是每个关键结论尽量附时间戳、原文片段和“事实/推断”标记。

10. 场景差异很大
    缺陷是会议、谈判、学习、维修需要的提示完全不同。优化方向是引入场景模式，不同模式使用不同的提示策略和报告模板。

## 重要边界

- 可以采集手机麦克风听到的周边声音。
- 不应依赖第三方 App 直接读取普通系统电话音频。
- 如果要像电话另一端一样参与对话，应做成自己的网络通话或实时语音会话。
- 不能把模型建议当作高风险决策的唯一依据。

## 落地阶段

1. 手机持续采音、实时识别、实时显示字幕。
2. 加入上下文管理，让模型结合短期窗口和长期摘要分析。
3. 加入事件检测和优先级过滤，避免每句话都打扰。
4. 加入实时建议、风险提醒和追问建议。
5. 加入会话存储、挂断后报告和历史记录。
6. 加入语音播报和耳机提醒。
7. 加入场景模式，例如会议、谈判、学习、维修。
8. 接入 AR 眼镜，把手机显示替换成信息浮窗。
9. 加入视觉输入，形成听觉 + 视觉的周边情况分析。

## 结论

这个方向值得做。它的核心价值不在“把声音转成文字”，而在于持续理解对话脉络，并在对话进行中给出有用提醒，同时在结束后沉淀成可靠记录。

最关键的设计原则是：

- 实时建议要少而准
- 不做默认聊天式 AI，而做旁路分析助手
- 上下文压缩要稳
- 结束后的报告要可查、可信、可导出
- 语音播报要可控，不能干扰真实对话
- 手机先独立跑通，再接 AR 眼镜
